import {StyleSheet} from 'react-native';

export const cardStyles = StyleSheet.create({
  cardContainer: {
    borderRadius: 10,
    elevation: 5,
    marginBottom: 5,
  },
  cardTitle: {
    fontSize: 20,
  },
});
